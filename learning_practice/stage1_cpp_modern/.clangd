# ===========================================================================
# 覆盖上级 talos-master/.clangd 的 CompilationDatabase 设置
# ---------------------------------------------------------------------------
# 上级 talos-master/.clangd 将编译数据库强制指向 talos 主项目的 build/，
# 导致本项目（stage1_cpp_modern）源文件在其中找不到编译记录，clangd 退回盲猜模式。
# 这里把编译数据库重新指向本项目自己的 build/compile_commands.json。
#
# 注意：本项目是 C++23（CDB 命令自带 -std=c++2b），这里【不要】Remove 标准标志，
# 否则 clangd 会退回默认 C++17 模式，concept/expected 等全部报错。
# ===========================================================================
CompileFlags:
  CompilationDatabase: build
